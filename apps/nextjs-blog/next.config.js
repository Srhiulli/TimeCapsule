/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'avatars.githubusercontent.com',
            '192.168.0.1',
            '192.168.0.11',
            'localhost'

        ],
    },
}

module.exports = nextConfig
